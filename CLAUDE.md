# splitpay - 開発ルール & エージェント運用フロー

## プロジェクト概要
- Swift/SwiftUI 製のiPhoneネイティブアプリ
- （技術詳細は実装が進み次第、随時このファイルに追記していく）

## タスクの取得元
タスク(Todo)は、MangaShelfと共有のNotionデータベース「漫画棚アプリ開発タスク」で管理する。
**このプロジェクトが対象とするのは、「プロジェクト」列が「splitpay」のタスクのみ。**
「プロジェクト」列が「MangaShelf」や空欄のタスクは絶対に処理しない（読み飛ばす）。

| プロパティ名 | 型 | 値の例 |
|---|---|---|
| 名前(タイトル) | Title | 「割り勘計算画面を追加」 |
| ステータス | Select | 未着手 / 進行中 / レビュー中 / テスト中 / デバッグ中 / 完了 / 要確認 |
| プロジェクト | Select | splitpay（これ以外は対象外） |
| メモ | Text | 補足指示（任意） |
| 実行結果 | Text | 処理完了後にClaudeが記入する要約 |
| 完了日時 | Last edited time | 自動記録 |

## 司令塔(このセッション)の動作フロー

`/loop` で定期実行される際、以下を行う:

1. Notionのタスクデータベースを確認する（ステータス=「未着手」**かつ** プロジェクト=「splitpay」の行のみ取得）
2. 該当タスクが無ければ何もせず終了する
3. 該当タスクが1件以上あれば、最も古いもの1件を選び、ステータスを「進行中」に更新してから処理を開始する

### 処理パイプライン（1タスクにつき）

```
1. dev-agent にタスク内容を渡して実装させる
2. review-agent にレビューさせる
	- "request_changes" の場合 → dev-agentに差し戻す（最大3回まで）
	- 3回で解決しなければ「要確認」にして次へ
3. review承認後、ステータスを「テスト中」に更新
4. test-agent にテストを実行させる
	- xcodebuildでビルドが通ること、可能であればシミュレータ起動確認を含む
	- FAILならdebug-agentに引き継ぐ（最大3ラウンドまで）
5. テストPASS後:
	- git commit（意味のある単位、日本語メッセージ）
	- git push
	- Notionステータスを「完了」に更新し、実行結果とページ本文に詳細ログを記録
```

## 実行結果の書き戻しルール
（MangaShelfと同様）
- 「実行結果」プロパティに短い要約
- ページ本文に各エージェントの詳細ログをトグルブロックで記録

## Git運用
- commit: 意味のある単位で自動実行してよい
- push: テストPASS後、自動実行してよい
- force push は禁止
- 新規Xcodeプロジェクトの大枠構成やFirebase等の外部サービス初期設定など、後戻りしにくい破壊的変更は実行前に一度立ち止まり、Remote Control経由でユーザーに確認を取る
